package com.example.wordbook.test

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class FirstTestResultViewModel (application: Application): AndroidViewModel(application) {

    /*private val viewModel: TestViewModel = ViewModelProvider.AndroidViewModelFactory.getInstance(application).create(TestViewModel::class.java)
    val correctCount: LiveData<Int> = viewModel.mTestcorrectCount*/


    /*private lateinit var viewModel: TestViewModel
    var correctcount = viewModel.mTestcorrectCount*/

}